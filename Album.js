class Album {
    /**
     * @param {String} artist Artist name 
     * @param {String} albumName Album name
     */
    constructor(artist, albumName) {
        this.artist = artist;
        this.albumName = albumName;
    }
}

module.exports = Album;